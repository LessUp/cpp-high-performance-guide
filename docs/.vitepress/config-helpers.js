export function futureSidebar(prefix) {
  return {
    [`${prefix}academy/`]: [],
    [`${prefix}architecture/`]: [],
    [`${prefix}reference/`]: [],
    [`${prefix}playbook/`]: [],
    [`${prefix}research/`]: [],
  }
}

export const englishReferenceHub = '/en/reference/'
