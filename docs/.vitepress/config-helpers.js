export function futureSidebar(prefix) {
  return {
    [`${prefix}academy/`]: [],
    [`${prefix}architecture/`]: [],
    [`${prefix}playbook/`]: [],
    [`${prefix}reference/`]: [],
    [`${prefix}research/`]: [],
  }
}
